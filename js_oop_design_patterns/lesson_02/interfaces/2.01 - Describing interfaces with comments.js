/*

interface Composite {
    function add(child);
    function remove(child);
    function getChild(index);
}

interface FormItem {
    function save();
}

*/

var CompositeForm = function() { // implements Composite, FormItem
    ...
};

// Implement the Composite interface.

/***
	What prorotype methods need to be created to impelement the Composite interface?
***/

// Implement the FormItem interface.

/***
	What prorotype methods need to be created to impelement the FormItem interface?
***/
